enableScripts: true

nodeLinker: node-modules

# Quarantine freshly-published package versions for 3 days before they can be
# resolved — guards against compromised-maintainer releases.
npmMinimalAgeGate: 4320

yarnPath: .yarn/releases/yarn-4.15.0.cjs
